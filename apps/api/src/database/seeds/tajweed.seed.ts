import { PrismaClient } from '@prisma/client';

export async function seedTajweedRules(prisma: PrismaClient) {
  console.log('📖 Seeding tajweed rules...');
  // TODO: Populate with verified scholar-reviewed tajweed rules in Sub-Step 2.2.2.1
  console.log('⏭️  Tajweed rules seed skipped (will be populated in Phase 2.2.2)');
}
