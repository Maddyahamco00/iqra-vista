import { PrismaClient } from '@prisma/client';

export async function seedTestUsers(prisma: PrismaClient) {
  console.log('👤 Seeding test users...');
  // TODO: Populate with test student, parent, admin in Sub-Step 2.2.3.1
  console.log('⏭️  Test users seed skipped (will be populated in Phase 2.2.3)');
}
