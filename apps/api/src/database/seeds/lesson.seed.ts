import { PrismaClient } from '@prisma/client';

export async function seedLessons(prisma: PrismaClient) {
  console.log('📖 Seeding lessons...');
  // TODO: Populate with curriculum lessons in Sub-Step 2.2.2.2
  console.log('⏭️  Lessons seed skipped (will be populated in Phase 2.2.2)');
}
